# exam_no 기반 시험 차수 관리 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** MySQL `std_list` 테이블의 `exam_no`를 읽어 태스크를 차수별로 분리하고, 시간표·실행 창·큐 전체에서 "(N차)"를 표시한다.

**Architecture:** MySQL 조회 결과를 `std_list_cache.json`에 캐시하고, 기존 `sync_test_data`가 이 캐시를 읽어 `(doc_id, exam_no)` 조합별로 태스크를 생성한다. 실행 레코드는 `(identifier_id, task_id)` 조합으로 스코프하여 같은 식별자가 여러 차수에 나타나더라도 독립 관리된다.

**Tech Stack:** PyMySQL 1.x, Flask, portalocker, pytest

---

## 파일 맵

| 파일 | 작업 |
|------|------|
| `requirements.txt` | `PyMySQL` 추가 |
| `app/features/schedule/models/std_list.py` | 신규: MySQL 조회 + 캐시 R/W |
| `app/features/schedule/data/std_list_cache.json` | 신규: 빈 캐시 파일 |
| `app/features/schedule/models/task.py` | `exam_no` 필드, `get_by_doc_and_exam`, `display_name` 헬퍼 |
| `app/features/schedule/services/sync.py` | `sync_test_data` — exam_no 기반 알고리즘 |
| `app/features/schedule/routes/sync.py` | `POST /api/sync/std-list` 엔드포인트 |
| `app/features/execution/models/execution.py` | `get_by_identifier_and_task` 추가 |
| `app/features/execution/routes/api.py` | `_build_item_dict` 업데이트, `/item` task_id 필터 |
| `app/features/schedule/helpers/enrichment.py` | 블록·큐에 `display_name`, `exam_no` 추가 |
| `app/templates/schedule/views/day.html` | `display_name` 사용 |
| `app/templates/schedule/views/week.html` | `display_name` 사용 |
| `app/templates/schedule/views/month.html` | `display_name` 사용 |
| `app/templates/schedule/views/_task_queue.html` | `display_name` 사용 |
| `app/templates/schedule/tasks/list.html` | 차수 표시 |
| `app/templates/schedule/tasks/detail.html` | 차수 표시 |
| `app/static/execution/js/execution-app.js` | `display_name` 사용 |
| `app/static/execution/js/execution-detail.js` | `display_name` 사용 |
| `tests/test_std_list.py` | 신규: std_list 모델 테스트 |
| `tests/test_sync.py` | exam_no 시나리오 테스트 추가 |

---

## Task 1: PyMySQL 설치 + std_list 모델

**Files:**
- Modify: `requirements.txt`
- Create: `app/features/schedule/models/std_list.py`
- Create: `app/features/schedule/data/std_list_cache.json`
- Create: `tests/test_std_list.py`

- [ ] **Step 1: requirements.txt에 PyMySQL 추가**

```
flask
portalocker
openpyxl
PyMySQL
pytest
```

- [ ] **Step 2: 테스트 파일 작성 (실패 확인용)**

`tests/test_std_list.py`:
```python
"""Tests for std_list model — MySQL cache and fetch."""
import pytest
from unittest.mock import patch, MagicMock


def test_load_cache_returns_empty_when_no_file(tmp_path, monkeypatch):
    """캐시 파일이 없으면 빈 리스트를 반환한다."""
    from app import create_app
    import os, json
    data_dir = str(tmp_path / 'data')
    os.makedirs(data_dir)
    for name in ('users', 'locations', 'tasks', 'schedule_blocks',
                 'versions', 'procedures', 'settings'):
        with open(os.path.join(data_dir, f'{name}.json'), 'w') as f:
            json.dump({} if name == 'settings' else [], f)
    app = create_app()
    app.config['DATA_DIR'] = data_dir
    app.config['TESTING'] = True
    with app.app_context():
        from app.features.schedule.models.std_list import load_cache
        assert load_cache() == []


def test_save_and_load_cache(tmp_path):
    """저장한 캐시를 다시 읽으면 동일한 데이터를 반환한다."""
    from app import create_app
    import os, json
    data_dir = str(tmp_path / 'data')
    os.makedirs(data_dir)
    for name in ('users', 'locations', 'tasks', 'schedule_blocks',
                 'versions', 'procedures'):
        with open(os.path.join(data_dir, f'{name}.json'), 'w') as f:
            json.dump([], f)
    with open(os.path.join(data_dir, 'settings.json'), 'w') as f:
        json.dump({
            'work_start': '08:00', 'work_end': '17:00',
            'actual_work_start': '08:30', 'actual_work_end': '16:30',
            'lunch_start': '12:00', 'lunch_end': '13:00',
            'breaks': [], 'grid_interval_minutes': 15,
            'max_schedule_days': 14, 'block_color_by': 'assignee',
        }, f)

    app = create_app()
    app.config['DATA_DIR'] = data_dir
    app.config['TESTING'] = True

    rows = [
        {'test_info': 'TC-001', 'exam_no': 1},
        {'test_info': 'TC-001', 'exam_no': 2},
    ]
    with app.app_context():
        from app.features.schedule.models.std_list import save_cache, load_cache
        save_cache(rows)
        assert load_cache() == rows


def test_fetch_from_mysql_returns_rows():
    """MySQL 연결 성공 시 test_info, exam_no 행을 반환한다."""
    mock_cursor = MagicMock()
    mock_cursor.__enter__ = lambda s: mock_cursor
    mock_cursor.__exit__ = MagicMock(return_value=False)
    mock_cursor.fetchall.return_value = [
        {'test_info': 'TC-001', 'exam_no': 1},
        {'test_info': 'TC-001', 'exam_no': 2},
        {'test_info': 'TC-002', 'exam_no': 1},
    ]
    mock_conn = MagicMock()
    mock_conn.cursor.return_value = mock_cursor

    with patch('app.features.schedule.models.std_list.pymysql.connect',
               return_value=mock_conn):
        from app.features.schedule.models import std_list
        import importlib; importlib.reload(std_list)
        result = std_list.fetch_from_mysql()

    assert len(result) == 3
    assert result[0] == {'test_info': 'TC-001', 'exam_no': 1}
    assert result[2] == {'test_info': 'TC-002', 'exam_no': 1}
```

- [ ] **Step 3: 테스트 실행 — 실패 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_std_list.py -v
```
Expected: `ModuleNotFoundError` 또는 `ImportError` (std_list 모듈 없음)

- [ ] **Step 4: `std_list.py` 구현**

`app/features/schedule/models/std_list.py`:
```python
import os
import pymysql
import pymysql.cursors

from app.features.schedule.store import read_json, write_json

CACHE_FILENAME = 'std_list_cache.json'


def _get_db_config():
    env = os.environ.get('FLASK_ENV', 'development')
    db_name = (
        os.environ.get('MYSQL_DB_PROD')
        if env == 'production'
        else os.environ.get('MYSQL_DB_DEV', '')
    )
    return {
        'host': os.environ.get('MYSQL_HOST', 'localhost'),
        'port': int(os.environ.get('MYSQL_PORT', '3306')),
        'user': os.environ.get('MYSQL_USER', ''),
        'password': os.environ.get('MYSQL_PASSWORD', ''),
        'database': db_name,
        'charset': 'utf8mb4',
        'cursorclass': pymysql.cursors.DictCursor,
    }


def fetch_from_mysql():
    """std_list 테이블에서 test_info, exam_no를 읽어 반환한다."""
    config = _get_db_config()
    conn = pymysql.connect(**config)
    try:
        with conn.cursor() as cursor:
            cursor.execute(
                'SELECT test_info, exam_no FROM std_list'
                ' WHERE exam_no IS NOT NULL'
            )
            return [
                {'test_info': row['test_info'], 'exam_no': row['exam_no']}
                for row in cursor.fetchall()
            ]
    finally:
        conn.close()


def load_cache():
    """로컬 캐시(std_list_cache.json)를 읽어 반환한다. 없으면 []."""
    return read_json(CACHE_FILENAME)


def save_cache(rows):
    """rows를 std_list_cache.json에 저장한다."""
    write_json(CACHE_FILENAME, rows)
```

- [ ] **Step 5: 빈 캐시 파일 생성**

`app/features/schedule/data/std_list_cache.json`:
```json
[]
```

- [ ] **Step 6: PyMySQL 설치**

```bash
source venv/bin/activate && pip install PyMySQL
```

- [ ] **Step 7: 테스트 실행 — 통과 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_std_list.py -v
```
Expected: 3 passed

- [ ] **Step 8: 커밋**

```bash
git add requirements.txt \
        app/features/schedule/models/std_list.py \
        app/features/schedule/data/std_list_cache.json \
        tests/test_std_list.py
git commit -m "feat(exam_no): std_list MySQL 모델 + 로컬 캐시"
```

---

## Task 2: /api/sync/std-list 엔드포인트

**Files:**
- Modify: `app/features/schedule/routes/sync.py`

- [ ] **Step 1: 테스트 추가 (test_sync.py 끝에 새 클래스 추가)**

`tests/test_sync.py` 파일 끝에 추가:
```python
# ===========================================================================
# TestSyncAPI (추가)
# ===========================================================================

class TestSyncStdListAPI:

    @pytest.fixture(autouse=True)
    def setup(self, tmp_path):
        self.app = _make_app(tmp_path)

    def test_sync_std_list_success(self):
        """MySQL 조회 성공 시 캐시를 저장하고 200을 반환한다."""
        from unittest.mock import patch
        fake_rows = [
            {'test_info': 'TC-001', 'exam_no': 1},
            {'test_info': 'TC-001', 'exam_no': 2},
        ]
        with self.app.test_client() as c:
            with patch(
                'app.features.schedule.models.std_list.fetch_from_mysql',
                return_value=fake_rows,
            ):
                r = c.post('/schedule/api/sync/std-list')
        assert r.status_code == 200
        data = r.get_json()
        assert data['cached'] == 2

    def test_sync_std_list_mysql_failure(self):
        """MySQL 접속 실패 시 503을 반환한다."""
        from unittest.mock import patch
        with self.app.test_client() as c:
            with patch(
                'app.features.schedule.models.std_list.fetch_from_mysql',
                side_effect=Exception('connection refused'),
            ):
                r = c.post('/schedule/api/sync/std-list')
        assert r.status_code == 503
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_sync.py::TestSyncStdListAPI -v
```
Expected: FAIL (엔드포인트 없음)

- [ ] **Step 3: sync.py에 엔드포인트 추가**

`app/features/schedule/routes/sync.py`의 기존 import 아래, `sync_versions` 함수 앞에 추가:

```python
import logging as _logging

_logger = _logging.getLogger(__name__)
```

파일 끝(현재 `sync_status` 함수 다음)에 추가:

```python
@sync_bp.route('/std-list', methods=['POST'])
def sync_std_list():
    """MySQL std_list 테이블에서 exam_no 정보를 가져와 로컬 캐시에 저장한다.

    Returns:
        JSON: {"cached": N} 성공, {"error": "..."} 503 실패
    """
    from app.features.schedule.models import std_list as std_list_model
    try:
        rows = std_list_model.fetch_from_mysql()
    except Exception as e:
        _logger.error('std_list MySQL 조회 실패: %s', e)
        return jsonify({'error': str(e)}), 503
    std_list_model.save_cache(rows)
    return jsonify({'cached': len(rows)})
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_sync.py::TestSyncStdListAPI -v
```
Expected: 2 passed

- [ ] **Step 5: 전체 기존 테스트 회귀 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_sync.py -v
```
Expected: 모두 통과

- [ ] **Step 6: 커밋**

```bash
git add app/features/schedule/routes/sync.py tests/test_sync.py
git commit -m "feat(exam_no): POST /api/sync/std-list 엔드포인트"
```

---

## Task 3: Task 모델 — exam_no 필드 + get_by_doc_and_exam

**Files:**
- Modify: `app/features/schedule/models/task.py`

- [ ] **Step 1: 테스트 추가 (tests/test_routes_tasks.py 끝에)**

`tests/test_routes_tasks.py` 파일 끝에 추가:
```python
class TestTaskExamNo:

    @pytest.fixture(autouse=True)
    def setup(self, tmp_path):
        import os, json
        from app import create_app
        data_dir = str(tmp_path / 'data')
        os.makedirs(data_dir)
        for name in ('users', 'locations', 'tasks', 'schedule_blocks',
                     'versions', 'procedures'):
            with open(os.path.join(data_dir, f'{name}.json'), 'w') as f:
                json.dump([], f)
        with open(os.path.join(data_dir, 'settings.json'), 'w') as f:
            json.dump({
                'work_start': '08:00', 'work_end': '17:00',
                'actual_work_start': '08:30', 'actual_work_end': '16:30',
                'lunch_start': '12:00', 'lunch_end': '13:00',
                'breaks': [], 'grid_interval_minutes': 15,
                'max_schedule_days': 14, 'block_color_by': 'assignee',
            }, f)
        self.app = create_app()
        self.app.config['DATA_DIR'] = data_dir
        self.app.config['TESTING'] = True

    def test_create_task_with_exam_no(self):
        """exam_no가 있는 태스크를 생성하면 해당 필드가 저장된다."""
        from app.features.schedule.models import task
        with self.app.app_context():
            t = task.create(
                doc_id=1, assignee_names=[], location_id='',
                doc_name='시스템 초기화',
                identifiers=[{'id': 'TC-001', 'estimated_minutes': 60, 'owners': []}],
                estimated_minutes=60, exam_no=2,
            )
            assert t['exam_no'] == 2

    def test_get_by_doc_and_exam(self):
        """(doc_id, exam_no) 조합으로 태스크를 조회한다."""
        from app.features.schedule.models import task
        with self.app.app_context():
            task.create(doc_id=1, assignee_names=[], location_id='',
                        doc_name='시스템', identifiers=[], estimated_minutes=0,
                        exam_no=1)
            task.create(doc_id=1, assignee_names=[], location_id='',
                        doc_name='시스템', identifiers=[], estimated_minutes=0,
                        exam_no=2)
            t1 = task.get_by_doc_and_exam(1, 1)
            t2 = task.get_by_doc_and_exam(1, 2)
            assert t1 is not None and t1['exam_no'] == 1
            assert t2 is not None and t2['exam_no'] == 2
            assert task.get_by_doc_and_exam(1, 99) is None

    def test_display_name_with_exam_no(self):
        """exam_no가 있으면 display_name에 차수 접미사가 붙는다."""
        from app.features.schedule.models import task as task_repo
        with self.app.app_context():
            t = task_repo.create(doc_id=1, assignee_names=[], location_id='',
                                 doc_name='시스템 초기화', identifiers=[],
                                 estimated_minutes=0, exam_no=3)
            assert task_repo.display_name(t) == '시스템 초기화 (3차)'

    def test_display_name_without_exam_no(self):
        """exam_no가 없으면 doc_name 그대로 반환한다."""
        from app.features.schedule.models import task as task_repo
        with self.app.app_context():
            t = task_repo.create(doc_id=2, assignee_names=[], location_id='',
                                 doc_name='항법 연산', identifiers=[],
                                 estimated_minutes=0)
            assert task_repo.display_name(t) == '항법 연산'
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_routes_tasks.py::TestTaskExamNo -v
```
Expected: FAIL (`exam_no` 필드 없음, `get_by_doc_and_exam` 없음)

- [ ] **Step 3: task.py 수정**

`app/features/schedule/models/task.py`의 `create` 메서드 시그니처와 data dict 수정:

```python
    @classmethod
    def create(cls, doc_id, assignee_names, location_id,
               doc_name, identifiers,
               estimated_minutes, memo='',
               version_id='', exam_no=None, **kwargs):
        data = {
            'doc_id': doc_id,
            'exam_no': exam_no,
            'version_id': version_id,
            'assignee_names': assignee_names or [],
            'location_id': location_id,
            'doc_name': doc_name,
            'identifiers': identifiers or [],
            'estimated_minutes': estimated_minutes,
            'remaining_minutes': estimated_minutes,
            'memo': memo,
            'created_at': datetime.now().isoformat(timespec='seconds'),
        }
        return super().create(data)
```

파일 끝(기존 `get_by_doc_id` 메서드 다음)에 추가:

```python
    @classmethod
    def get_by_doc_and_exam(cls, doc_id, exam_no):
        """(doc_id, exam_no) 조합으로 태스크를 조회한다.

        exam_no=None이면 exam_no 필드가 없거나 None인 태스크를 찾는다.

        Args:
            doc_id: 문서 ID (int 또는 변환 가능한 값)
            exam_no: 시험 차수 (int 또는 None)

        Returns:
            dict 또는 None: 일치하는 태스크, 없으면 None
        """
        try:
            target = int(doc_id)
        except (TypeError, ValueError):
            return None
        for t in cls.get_all():
            if t.get('doc_id') == target and t.get('exam_no') == exam_no:
                return t
        return None

    @staticmethod
    def display_name(task_dict):
        """태스크의 표시명을 반환한다.

        exam_no가 있으면 'doc_name (N차)', 없으면 doc_name.
        """
        name = task_dict.get('doc_name', '')
        exam_no = task_dict.get('exam_no')
        if exam_no is not None:
            return f'{name} ({exam_no}차)'
        return name
```

모듈 수준 노출(파일 맨 끝):
```python
get_by_doc_and_exam = TaskRepository.get_by_doc_and_exam
display_name = TaskRepository.display_name
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_routes_tasks.py::TestTaskExamNo -v
```
Expected: 4 passed

- [ ] **Step 5: 전체 기존 태스크 테스트 회귀 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_routes_tasks.py -v
```
Expected: 모두 통과

- [ ] **Step 6: 커밋**

```bash
git add app/features/schedule/models/task.py tests/test_routes_tasks.py
git commit -m "feat(exam_no): 태스크 exam_no 필드 + get_by_doc_and_exam + display_name"
```

---

## Task 4: sync_test_data — exam_no 기반 태스크 생성

**Files:**
- Modify: `app/features/schedule/services/sync.py`

- [ ] **Step 1: 테스트 추가 (tests/test_sync.py의 TestSyncTestData 클래스에)**

`tests/test_sync.py`의 `TestSyncTestData` 클래스 끝에 추가:

```python
    def test_sync_creates_exam_no_tasks_from_cache(self):
        """std_list 캐시가 있으면 (doc_id, exam_no) 조합별로 태스크를 생성한다."""
        from unittest.mock import patch

        class MockProvider(BaseProvider):
            def get_versions(self): return []
            def get_test_data(self, version_id): return []
            def get_test_data_all(self):
                return [{
                    'doc_id': 1,
                    'doc_name': '시스템 초기화',
                    'version_id': 'VER-001',
                    'identifiers': [
                        {'id': 'TC-001', 'estimated_minutes': 60, 'owners': []},
                        {'id': 'TC-002', 'estimated_minutes': 90, 'owners': []},
                    ],
                }]

        fake_cache = [
            {'test_info': 'TC-001', 'exam_no': 1},
            {'test_info': 'TC-001', 'exam_no': 2},
            {'test_info': 'TC-002', 'exam_no': 1},
        ]

        with self.app.app_context():
            with patch(
                'app.features.schedule.services.sync.load_std_list_cache',
                return_value=fake_cache,
            ):
                result = SyncService.sync_test_data(MockProvider())

            assert result['added'] == 2  # exam_no=1(TC-001,TC-002), exam_no=2(TC-001)
            tasks = task.get_all()
            assert len(tasks) == 2

            exam1 = next(t for t in tasks if t.get('exam_no') == 1)
            exam2 = next(t for t in tasks if t.get('exam_no') == 2)

            assert len(exam1['identifiers']) == 2  # TC-001, TC-002
            assert len(exam2['identifiers']) == 1  # TC-001만

    def test_sync_no_cache_creates_single_task(self):
        """std_list 캐시가 비어 있으면 exam_no=None 태스크 1개를 생성한다."""
        from unittest.mock import patch

        class MockProvider(BaseProvider):
            def get_versions(self): return []
            def get_test_data(self, version_id): return []
            def get_test_data_all(self):
                return [{
                    'doc_id': 2,
                    'doc_name': '항법 연산',
                    'version_id': 'VER-001',
                    'identifiers': [
                        {'id': 'TC-010', 'estimated_minutes': 30, 'owners': []},
                    ],
                }]

        with self.app.app_context():
            with patch(
                'app.features.schedule.services.sync.load_std_list_cache',
                return_value=[],
            ):
                result = SyncService.sync_test_data(MockProvider())

            assert result['added'] == 1
            tasks = task.get_all()
            assert len(tasks) == 1
            assert tasks[0].get('exam_no') is None

    def test_sync_cancels_old_exam_no_none_when_cache_appears(self):
        """기존 exam_no=None 태스크가 있을 때 캐시가 생기면 자동 cancelled 처리한다."""
        from unittest.mock import patch

        class MockProvider(BaseProvider):
            def get_versions(self): return []
            def get_test_data(self, version_id): return []
            def get_test_data_all(self):
                return [{
                    'doc_id': 1,
                    'doc_name': '시스템',
                    'version_id': 'VER-001',
                    'identifiers': [
                        {'id': 'TC-001', 'estimated_minutes': 60, 'owners': []},
                    ],
                }]

        with self.app.app_context():
            # 먼저 exam_no=None 태스크 생성
            with patch('app.features.schedule.services.sync.load_std_list_cache',
                       return_value=[]):
                SyncService.sync_test_data(MockProvider())

            old_task = task.get_all()[0]
            assert old_task.get('exam_no') is None

            # 이번에는 캐시에 exam_no 데이터가 생김
            fake_cache = [{'test_info': 'TC-001', 'exam_no': 1}]
            with patch('app.features.schedule.services.sync.load_std_list_cache',
                       return_value=fake_cache):
                SyncService.sync_test_data(MockProvider())

            tasks = task.get_all()
            # exam_no=None 태스크는 cancelled, exam_no=1 태스크가 새로 생성됨
            cancelled = [t for t in tasks if t.get('status') == 'cancelled']
            active = [t for t in tasks if t.get('status') != 'cancelled']
            assert len(cancelled) == 1
            assert len(active) == 1
            assert active[0]['exam_no'] == 1
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_sync.py::TestSyncTestData -v -k "exam_no or cache"
```
Expected: FAIL (`load_std_list_cache` 없음)

- [ ] **Step 3: sync.py 전체 교체**

`app/features/schedule/services/sync.py`:
```python
"""동기화 서비스 — 외부 프로바이더 데이터를 로컬 모델에 병합한다.

외부 데이터 소스(프로바이더)로부터 버전 정보와 시험 데이터를 가져와
로컬 JSON 파일 기반 모델에 추가/갱신/비활성화하는 기능을 제공한다.
exam_no 기반 태스크 분리: std_list_cache.json의 데이터를 참조하여
(doc_id, exam_no) 조합별로 독립 태스크를 생성한다.
"""

from app.features.schedule.models import version, task
from app.config import OfpidSettings


def load_std_list_cache():
    """std_list 로컬 캐시를 읽어 반환한다. 없으면 []."""
    from app.features.schedule.models.std_list import load_cache
    return load_cache()


class SyncService:
    """외부 프로바이더와의 데이터 동기화를 수행하는 서비스 클래스."""

    @staticmethod
    def sync_versions(provider):
        """프로바이더로부터 버전 정보를 동기화한다."""
        external = provider.get_versions()
        external_ids = {v['id'] for v in external}
        existing = {v['id']: v for v in version.get_all()}
        added = updated = deactivated = 0

        for ext in external:
            if ext['id'] in existing:
                version.update(ext['id'], name=ext['name'],
                               description=ext.get('description', ''),
                               is_active=True)
                updated += 1
            else:
                version.create(name=ext['name'],
                               description=ext.get('description', ''),
                               id=ext['id'])
                added += 1

        for vid, v in existing.items():
            if vid not in external_ids and v.get('is_active', True):
                version.patch(vid, is_active=False)
                deactivated += 1

        return {'added': added, 'updated': updated, 'deactivated': deactivated}

    @staticmethod
    def sync_test_data(provider, version_id=None):
        """프로바이더로부터 시험 데이터를 동기화한다.

        std_list_cache.json의 exam_no 정보를 참조하여
        (doc_id, exam_no) 조합별로 태스크를 생성·갱신한다.
        캐시가 비어 있으면 exam_no=None 태스크 1개로 기존 방식대로 동작한다.

        Args:
            provider: BaseProvider 인스턴스.
            version_id: 특정 버전 ID로 제한. None이면 전체.

        Returns:
            dict: {'added': int, 'updated': int, 'cancelled': int, 'warnings': list}
        """
        # exam_no 맵 구성: test_info → {exam_no, ...}
        exam_no_map = {}
        for row in load_std_list_cache():
            ti = row.get('test_info', '')
            en = row.get('exam_no')
            if ti and en is not None:
                exam_no_map.setdefault(ti, set()).add(en)

        if version_id:
            external = provider.get_test_data(version_id)
        else:
            external = provider.get_test_data_all()

        synced_combos = set()  # {(doc_id, exam_no)} 이번 sync에서 처리한 조합
        added = updated = cancelled = 0
        warnings = []

        for item in external:
            try:
                doc_id = int(item.get('doc_id'))
            except (TypeError, ValueError):
                warnings.append(f"잘못된 doc_id, 건너뜀: {item}")
                continue

            doc_name = item.get('doc_name') or item.get('section_name', '')
            identifiers = item.get('identifiers', [])
            ver = (item.get('version_id')
                   or OfpidSettings.get_current_ofp_id()
                   or '')

            # 이 문서의 식별자들이 갖는 exam_no 집합
            doc_exam_nos = set()
            for ident in identifiers:
                ident_id = ident.get('id', '') if isinstance(ident, dict) else ident
                doc_exam_nos.update(exam_no_map.get(ident_id, set()))

            if not doc_exam_nos:
                # exam_no 정보 없음 → exam_no=None 태스크 1개
                combos_to_sync = [(None, identifiers)]
            else:
                combos_to_sync = []
                for exam_no in sorted(doc_exam_nos):
                    filtered = [
                        i for i in identifiers
                        if isinstance(i, dict)
                        and exam_no in exam_no_map.get(i.get('id', ''), set())
                    ]
                    combos_to_sync.append((exam_no, filtered))

            for exam_no, idents in combos_to_sync:
                est_minutes = sum(
                    i.get('estimated_minutes', 0)
                    for i in idents
                    if isinstance(i, dict)
                )
                synced_combos.add((doc_id, exam_no))

                existing = task.get_by_doc_and_exam(doc_id, exam_no)
                if existing:
                    task.patch(existing['id'],
                               identifiers=idents,
                               estimated_minutes=est_minutes,
                               doc_name=doc_name,
                               version_id=ver)
                    updated += 1
                else:
                    task.create(
                        doc_id=doc_id,
                        exam_no=exam_no,
                        version_id=ver,
                        assignee_names=[],
                        location_id='',
                        doc_name=doc_name,
                        identifiers=idents,
                        estimated_minutes=est_minutes,
                    )
                    added += 1

        # 이번 sync 결과에 없는 (doc_id, exam_no) 조합을 취소 처리
        for t in task.get_all():
            did = t.get('doc_id')
            if did is None:
                continue
            combo = (did, t.get('exam_no'))
            if combo not in synced_combos and t.get('status') != 'cancelled':
                task.patch(t['id'], status='cancelled')
                cancelled += 1

        return {'added': added, 'updated': updated,
                'cancelled': cancelled, 'warnings': warnings}
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_sync.py -v
```
Expected: 모두 통과 (기존 3 + 신규 3 + API 2)

- [ ] **Step 5: 커밋**

```bash
git add app/features/schedule/services/sync.py tests/test_sync.py
git commit -m "feat(exam_no): sync_test_data exam_no 기반 태스크 분리"
```

---

## Task 5: Execution 모델 — get_by_identifier_and_task

**Files:**
- Modify: `app/features/execution/models/execution.py`

- [ ] **Step 1: 테스트 작성**

`tests/test_routes_calendar.py`에서 기존 `_make_app` 패턴을 참고. `tests/` 디렉터리에 `test_execution_model.py` 신규 작성:

```python
"""Tests for ExecutionRepository.get_by_identifier_and_task."""
import json, os
import pytest
from app import create_app


def _make_app(tmp_path):
    data_dir = str(tmp_path / 'sched_data')
    exec_dir = str(tmp_path / 'exec_data')
    os.makedirs(data_dir)
    os.makedirs(exec_dir)
    for name in ('users', 'locations', 'tasks', 'schedule_blocks', 'versions', 'procedures'):
        with open(os.path.join(data_dir, f'{name}.json'), 'w') as f:
            json.dump([], f)
    with open(os.path.join(data_dir, 'settings.json'), 'w') as f:
        json.dump({
            'work_start': '08:00', 'work_end': '17:00',
            'actual_work_start': '08:30', 'actual_work_end': '16:30',
            'lunch_start': '12:00', 'lunch_end': '13:00',
            'breaks': [], 'grid_interval_minutes': 15,
            'max_schedule_days': 14, 'block_color_by': 'assignee',
        }, f)
    with open(os.path.join(exec_dir, 'executions.json'), 'w') as f:
        json.dump([], f)
    app = create_app()
    app.config['DATA_DIR'] = data_dir
    app.config['EXEC_DATA_DIR'] = exec_dir
    app.config['TESTING'] = True
    return app


class TestGetByIdentifierAndTask:

    @pytest.fixture(autouse=True)
    def setup(self, tmp_path):
        self.app = _make_app(tmp_path)

    def test_returns_correct_execution_by_task_scope(self):
        """동일 identifier_id가 두 태스크에 있을 때 task_id로 올바른 실행을 반환한다."""
        from app.features.execution.models.execution import ExecutionRepository
        with self.app.app_context():
            ex1 = ExecutionRepository.start('TC-001', 't_task1', total_count=10)
            ex2 = ExecutionRepository.start('TC-001', 't_task2', total_count=10)

            found1 = ExecutionRepository.get_by_identifier_and_task('TC-001', 't_task1')
            found2 = ExecutionRepository.get_by_identifier_and_task('TC-001', 't_task2')

            assert found1 is not None
            assert found1['task_id'] == 't_task1'
            assert found2 is not None
            assert found2['task_id'] == 't_task2'
            assert found1['id'] != found2['id']

    def test_returns_none_when_not_found(self):
        """일치하는 레코드가 없으면 None을 반환한다."""
        from app.features.execution.models.execution import ExecutionRepository
        with self.app.app_context():
            result = ExecutionRepository.get_by_identifier_and_task('TC-999', 't_none')
            assert result is None
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_execution_model.py -v
```
Expected: FAIL (`get_by_identifier_and_task` 없음, 또는 execution 시작이 기존 레코드를 덮어써서 2개 미생성)

- [ ] **Step 3: execution.py에 메서드 추가**

`app/features/execution/models/execution.py`의 `get_by_identifier` 메서드 바로 뒤에 추가:

```python
    @classmethod
    def get_by_identifier_and_task(cls, identifier_id: str, task_id: str):
        """(identifier_id, task_id) 조합으로 실행 레코드를 조회한다.

        같은 식별자가 여러 차수(exam_no) 태스크에 존재할 때
        올바른 실행 레코드를 찾기 위해 task_id를 추가 조건으로 사용한다.

        Args:
            identifier_id: 식별자 ID (예: 'TC-001')
            task_id: 태스크 ID (예: 't_abc123')

        Returns:
            dict 또는 None: 일치하는 실행 레코드, 없으면 None
        """
        for item in read_json(FILENAME):
            if (item['identifier_id'] == identifier_id
                    and item.get('task_id') == task_id):
                return item
        return None
```

- [ ] **Step 4: execution.py의 start() 메서드에서 기존 레코드 조회 방식 수정**

`app/features/execution/models/execution.py` 94번째 줄 `start` 메서드에서 111번째 줄:

기존:
```python
        existing = cls.get_by_identifier(identifier_id)
```
변경:
```python
        existing = cls.get_by_identifier_and_task(identifier_id, task_id)
```

이렇게 하면 TC-001이 두 태스크(exam_no=1, 2)에 있어도 각 태스크별 독립 실행 레코드를 생성한다. 241번째 줄(`pending_comment` 경로의 `get_by_identifier`)은 task_id 컨텍스트가 없는 경로이므로 그대로 유지한다.

- [ ] **Step 5: 테스트 실행 — 통과 확인**

```bash
source venv/bin/activate && python -m pytest tests/test_execution_model.py -v
```
Expected: 2 passed

- [ ] **Step 6: 커밋**

```bash
git add app/features/execution/models/execution.py tests/test_execution_model.py
git commit -m "feat(exam_no): get_by_identifier_and_task — 실행 스코프를 task_id로 한정"
```

---

## Task 6: Execution API — _build_item_dict 업데이트

**Files:**
- Modify: `app/features/execution/routes/api.py`

- [ ] **Step 1: _build_item_dict 수정**

`app/features/execution/routes/api.py`의 `_build_item_dict` 함수 전체를 교체:

```python
def _build_item_dict(task, identifier, locations, scheduled_date, block_loc_id=''):
    """태스크·식별자·스케줄·실행 데이터를 하나의 응답 딕셔너리로 조립한다."""
    iid = identifier['id']
    loc_id = block_loc_id or task.get('location_id', '')
    loc_name = locations.get(loc_id, {}).get('name', '') if loc_id else ''
    # task_id로 스코프된 실행 레코드 조회 (exam_no 지원)
    execution = ExecutionRepository.get_by_identifier_and_task(iid, task['id'])
    exam_no = task.get('exam_no')
    doc_name = task.get('doc_name', '')
    display_name = f'{doc_name} ({exam_no}차)' if exam_no is not None else doc_name
    return {
        'identifier_id': iid,
        'identifier_name': identifier.get('name', ''),
        'task_id': task['id'],
        'exam_no': exam_no,
        'doc_name': doc_name,
        'display_name': display_name,
        'assignee_names': task.get('assignee_names', []),
        'estimated_minutes': identifier.get('estimated_minutes', 0),
        'location_id': loc_id,
        'location_name': loc_name,
        'scheduled_date': scheduled_date,
        'execution': _execution_response(execution),
    }
```

- [ ] **Step 2: /item/<identifier_id> 엔드포인트에 task_id 필터 추가**

`app/features/execution/routes/api.py`의 `get_item` 함수 첫 줄에 추가:

```python
@api_bp.route('/item/<identifier_id>')
def get_item(identifier_id):
    """단일 식별자의 상세 실행 정보를 반환한다.

    쿼리 파라미터:
        task_id (str, optional): 특정 태스크로 스코프. 미지정 시 첫 번째 일치.
    """
    task_id_filter = request.args.get('task_id', '')
    tasks, locations, date_map, block_loc_map = _load_schedule_data()
    for task in tasks:
        if task_id_filter and task['id'] != task_id_filter:
            continue
        for identifier in task.get('identifiers', []):
            if not isinstance(identifier, dict):
                continue
            if identifier['id'] != identifier_id:
                continue
            return jsonify(_build_item_dict(
                task, identifier, locations,
                date_map.get(identifier_id, ''),
                block_loc_map.get(identifier_id, ''),
            ))
    return jsonify({'error': 'not found'}), 404
```

- [ ] **Step 3: get_by_identifier_and_task import 확인**

`api.py` 상단의 import에 이미 `ExecutionRepository`가 있으므로 추가 import 불필요.

- [ ] **Step 4: 기존 execution API 테스트 실행**

```bash
source venv/bin/activate && python -m pytest tests/ -v -k "execution or calendar"
```
Expected: 모두 통과

- [ ] **Step 5: 커밋**

```bash
git add app/features/execution/routes/api.py
git commit -m "feat(exam_no): execution API — display_name·exam_no 응답 + task_id 스코프"
```

---

## Task 7: Enrichment — display_name, exam_no 블록·큐에 추가

**Files:**
- Modify: `app/features/schedule/helpers/enrichment.py`

- [ ] **Step 1: enrich_blocks에 display_name과 exam_no 추가**

`app/features/schedule/helpers/enrichment.py`의 블록 루프에서 `block['task_title']` 설정 직후(약 120번째 줄)에 추가:

```python
        # exam_no 및 표시명 추가
        exam_no = t.get('exam_no') if t else None
        block['exam_no'] = exam_no
        if is_simple:
            block['display_name'] = b.get('title', '(블록)')
        else:
            base = t.get('doc_name', '') if t else ''
            block['display_name'] = f'{base} ({exam_no}차)' if exam_no is not None else base
```

- [ ] **Step 2: build_task_queue에 display_name과 exam_no 추가**

`app/features/schedule/helpers/enrichment.py`의 큐 빌드 함수에서 `task_item = dict(t)` 이후(약 286번째 줄)에 추가:

```python
        exam_no = t.get('exam_no')
        task_item['exam_no'] = exam_no
        doc_nm = t.get('doc_name', '')
        task_item['display_name'] = f'{doc_nm} ({exam_no}차)' if exam_no is not None else doc_nm
```

- [ ] **Step 3: 큐 정렬 키 수정 (display_name 기준)**

`enrichment.py`의 큐 정렬 라인을 찾아 수정:

기존:
```python
    queue.sort(key=lambda t: t.get('doc_name', '') or str(t.get('doc_id', '')))
```
변경:
```python
    queue.sort(key=lambda t: (t.get('doc_name', '') or str(t.get('doc_id', '')),
                               t.get('exam_no') or 0))
```

- [ ] **Step 4: 기존 테스트 회귀 확인**

```bash
source venv/bin/activate && python -m pytest tests/ -v
```
Expected: 모두 통과

- [ ] **Step 5: 커밋**

```bash
git add app/features/schedule/helpers/enrichment.py
git commit -m "feat(exam_no): enrichment에 display_name·exam_no 필드 추가"
```

---

## Task 8: 시간표 템플릿 — display_name 적용

**Files:**
- Modify: `app/templates/schedule/views/day.html`
- Modify: `app/templates/schedule/views/week.html`
- Modify: `app/templates/schedule/views/month.html`
- Modify: `app/templates/schedule/views/_task_queue.html`
- Modify: `app/templates/schedule/tasks/list.html`
- Modify: `app/templates/schedule/tasks/detail.html`

- [ ] **Step 1: day.html — 블록 라벨 수정**

`app/templates/schedule/views/day.html`에서:

기존:
```html
title="{{ block.doc_name or block.task_title }} ({{ block.assignee_name }}) {{ block.start_time }}–{{ block.end_time }}"
```
변경:
```html
title="{{ block.display_name or block.task_title }} ({{ block.assignee_name }}) {{ block.start_time }}–{{ block.end_time }}"
```

기존:
```html
{{ block.doc_name or block.doc_id }}
```
변경:
```html
{{ block.display_name or block.doc_id }}
```

- [ ] **Step 2: week.html — 블록 라벨 수정**

`app/templates/schedule/views/week.html`에서:

기존:
```html
title="{{ block.doc_name or block.task_title }} ({{ block.assignee_name }}) {{ block.start_time }}–{{ block.end_time }}"
```
변경:
```html
title="{{ block.display_name or block.task_title }} ({{ block.assignee_name }}) {{ block.start_time }}–{{ block.end_time }}"
```

기존(블록 내용 텍스트):
```html
{% endif %}{{ block.doc_name or block.task_title }}{% if block.is_split %}
```
변경:
```html
{% endif %}{{ block.display_name or block.task_title }}{% if block.is_split %}
```

- [ ] **Step 3: month.html — 블록 라벨 수정**

`app/templates/schedule/views/month.html`에서 동일하게 `block.doc_name`을 `block.display_name`으로 교체 (2곳).

- [ ] **Step 4: _task_queue.html — 큐 카드 수정**

`app/templates/schedule/views/_task_queue.html`에서:

기존:
```html
data-title="{{ task.doc_name or task.doc_id }}"
```
변경:
```html
data-title="{{ task.display_name or task.doc_id }}"
```

기존:
```html
<span class="queue-card-section-title">{{ task.doc_name }}</span>
```
변경:
```html
<span class="queue-card-section-title">{{ task.display_name or task.doc_name }}</span>
```

- [ ] **Step 5: tasks/list.html — 차수 표시**

`app/templates/schedule/tasks/list.html`에서 `task.doc_name`이 표시되는 곳(약 111번째 줄)을 찾아:

기존:
```html
{{ task.doc_name or '-' }}
```
변경:
```html
{{ task.display_name or task.doc_name or '-' }}
```

단, `task.display_name`은 태스크 목록 라우트에서 아직 없으므로, `app/features/schedule/routes/tasks.py`의 `task_list` 함수에서 231번째 줄 `return render_template(...)` 바로 앞에 추가:

```python
    from app.features.schedule.models.task import display_name as make_display_name
    for t in tasks_all:
        t['display_name'] = make_display_name(t)
    return render_template('schedule/tasks/list.html', ...)
```

마찬가지로 `task_detail` 함수(339번째 줄 render_template 앞)에도 추가:
```python
    t['display_name'] = make_display_name(t)
```

- [ ] **Step 6: tasks/detail.html — 차수 표시**

`app/templates/schedule/tasks/detail.html`에서 문서명 표시 부분에 차수 뱃지 추가:

기존:
```html
<h2>{{ task.doc_name or '' }}</h2>
```
변경:
```html
<h2>{{ task.doc_name or '' }}{% if task.exam_no is not none %} <span class="badge bg-secondary">{{ task.exam_no }}차</span>{% endif %}</h2>
```

기존:
```html
<tr><th style="width:140px">문서명</th><td class="fw-bold">{{ task.doc_name or '-' }}</td></tr>
```
변경:
```html
<tr><th style="width:140px">문서명</th><td class="fw-bold">{{ task.doc_name or '-' }}{% if task.exam_no is not none %} <span class="badge bg-secondary ms-1">{{ task.exam_no }}차</span>{% endif %}</td></tr>
```

- [ ] **Step 7: 전체 테스트 실행**

```bash
source venv/bin/activate && python -m pytest tests/ -v
```
Expected: 모두 통과

- [ ] **Step 8: 커밋**

```bash
git add app/templates/schedule/views/day.html \
        app/templates/schedule/views/week.html \
        app/templates/schedule/views/month.html \
        app/templates/schedule/views/_task_queue.html \
        app/templates/schedule/tasks/list.html \
        app/templates/schedule/tasks/detail.html \
        app/features/schedule/routes/tasks.py
git commit -m "feat(exam_no): 시간표·큐·태스크 목록 display_name 적용"
```

---

## Task 9: Execution JS — display_name 적용

**Files:**
- Modify: `app/static/execution/js/execution-app.js`
- Modify: `app/static/execution/js/execution-detail.js`

- [ ] **Step 1: execution-app.js — doc_name → display_name**

`app/static/execution/js/execution-app.js`에서 `item.doc_name`을 사용하는 곳(약 389번째 줄):

기존:
```javascript
cells.push(`<td class="td-doc">${escHtml(item.doc_name || '-')}</td>`);
```
변경:
```javascript
cells.push(`<td class="td-doc">${escHtml(item.display_name || item.doc_name || '-')}</td>`);
```

- [ ] **Step 2: execution-detail.js — doc_name → display_name**

`app/static/execution/js/execution-detail.js`에서 `item.doc_name`을 사용하는 곳(약 255번째 줄):

기존:
```javascript
<div class="sidebar-field-value">${escHtml(item.doc_name || '-')}</div>
```
변경:
```javascript
<div class="sidebar-field-value">${escHtml(item.display_name || item.doc_name || '-')}</div>
```

- [ ] **Step 3: 전체 테스트 최종 실행**

```bash
source venv/bin/activate && python -m pytest tests/ -v
```
Expected: 모두 통과

- [ ] **Step 4: 최종 커밋**

```bash
git add app/static/execution/js/execution-app.js \
        app/static/execution/js/execution-detail.js
git commit -m "feat(exam_no): 실행 창 display_name 적용"
```

---

## 검증 체크리스트

- [ ] `POST /schedule/api/sync/std-list` → MySQL 조회 → 캐시 저장
- [ ] `POST /schedule/api/sync/test-data` → std_list 캐시 반영 → 차수별 태스크 분리
- [ ] 태스크 큐에 "(1차)", "(2차)" 뱃지 표시
- [ ] 시간표 블록 라벨에 차수 표시
- [ ] 실행 창 목록에 차수 표시
- [ ] 같은 식별자 두 태스크에서 각각 독립 실행 레코드 생성
- [ ] 캐시 없을 때 기존 동작 그대로 (하위 호환)
- [ ] 전체 테스트 통과
